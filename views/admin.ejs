<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8" />
  <title>Адмінка</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    form { margin-bottom: 40px; }
    input[type=text], input[type=file], button {
      display: block; margin: 10px 0; padding: 8px; width: 300px;
    }
    button {
      background: #28a745; color: white; border: none;
      cursor: pointer;
      border-radius: 4px;
    }
    button:hover { background: #218838; }
    ul { list-style: none; padding: 0; }
    li { margin-bottom: 20px; }
    img { max-width: 200px; display: block; margin-bottom: 5px; }
  </style>
</head>
<body>
  <h1>Адмінка - Додати пост</h1>
  <form action="/add" method="POST" enctype="multipart/form-data">
    <input type="file" name="image" accept="image/*" required />
    <input type="text" name="caption" placeholder="Підпис до зображення" required />
    <button type="submit">Додати</button>
  </form>

  <h2>Всі пости</h2>
  <% if (posts.length === 0) { %>
    <p>Постів немає.</p>
  <% } else { %>
    <ul>
      <% posts.forEach(post => { %>
        <li>
          <img src="/uploads/<%= post.image %>" alt="Зображення поста" />
          <strong><%= post.caption %></strong><br/>
          <small><%= post.date %></small>
        </li>
      <% }) %>
    </ul>
  <% } %>

  <a href="/">На головну</a> | <a href="/logout">Вийти</a>
</body>
</html>
