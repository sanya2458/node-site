<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8" />
  <title>Головна сторінка</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 0; padding: 20px; }
    header { display: flex; justify-content: flex-end; margin-bottom: 20px; }
    header a { text-decoration: none; background: #007bff; color: white; padding: 8px 16px; border-radius: 4px; }
    ul { list-style: none; padding: 0; }
    li { margin-bottom: 20px; }
    img { max-width: 300px; display: block; margin-bottom: 8px; }
  </style>
</head>
<body>
  <header>
    <% if (isAdmin) { %>
      <a href="/admin">Адмінка</a>
      <a href="/logout" style="margin-left: 10px; background: #dc3545;">Вийти</a>
    <% } else { %>
      <a href="/login">Увійти</a>
    <% } %>
  </header>

  <h1>Пости</h1>

  <% if (posts.length === 0) { %>
    <p>Постів немає.</p>
  <% } else { %>
    <ul>
      <% posts.forEach(post => { %>
        <li>
          <img src="/uploads/<%= post.image %>" alt="Зображення поста" />
          <strong><%= post.caption %></strong><br/>
          <small><%= post.date %></small>
        </li>
      <% }) %>
    </ul>
  <% } %>
</body>
</html>

    <h1>Ласкаво просимо на сайт!</h1>
    <!-- Тут твій основний контент -->
  </main>
</body>
</html>
